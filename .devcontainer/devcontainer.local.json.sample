{
  // Local overrides for Dev Container (not committed)
  // Rename to: .devcontainer/devcontainer.local.json
  "containerEnv": {
    // Disable secure firewall (default is true)
    // "SECURE_MODE": "false",

    // Additional allowlist (comma/space-separated or CIDR)
    // Example for private registries or mirrors:
    "ADDITIONAL_ALLOWED_DOMAINS": "nexus.company.com, artifactory.internal.net"
  }
}
